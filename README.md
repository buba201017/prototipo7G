
  # Maleta Management System

  This is a code bundle for Maleta Management System. The original project is available at https://www.figma.com/design/b447Ua3nYE0qpbq1WojFgq/Maleta-Management-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  